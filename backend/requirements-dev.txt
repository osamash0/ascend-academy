# Install on top of requirements.txt for running the test suite:
#   pip install -r backend/requirements.txt -r backend/requirements-dev.txt
-r requirements.txt

# ── Test framework ────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0

# ── HTTP mocking / time travel / fakes ───────────────────────────────────────
respx>=0.21
freezegun>=1.5
faker>=24.0
httpx>=0.27

# ── DB / RLS tests (gated behind `pytest -m db`) ─────────────────────────────
# psycopg drives the throwaway Postgres container;
# testcontainers boots and tears it down per session.
psycopg[binary]>=3.1
testcontainers[postgres]>=4.0
