# Auto-provisions the local Prometheus as Grafana's default datasource so a
# fresh `docker compose up` doesn't need manual datasource setup.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
