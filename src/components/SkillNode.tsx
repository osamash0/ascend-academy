/**
 * SkillNode — now a stub.
 * The full skill-tree rendering is done natively in SVG inside SkillTreeView.tsx.
 * This file is retained so any legacy import paths don't break.
 */
export const BOX_W = 160;
export const BOX_H = 120;

// No-op export — SkillTreeView no longer uses this component.
export function SkillNode() {
  return null;
}
