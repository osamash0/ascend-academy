# Notes · Spaces screen — known gaps
*Feeds into Doc 2 (Navigation & Modes), states section. Recorded 2026-08-29.*

1. **Empty states** — zero Spaces (every new user): one clear action, "Create your first Space / Join with code," plain words. Also: Discover empty at a small Universe.
2. **Define "new"** — badge must mean exactly one thing. Rec: new *published* content since last visit. Otherwise remove.
3. **Role signal** — mark owned Spaces ("Owner" chip or "Created by you" group). Creators must spot their Spaces instantly.
4. **Public preview before join** — tapping a Discover card opens a preview (lecture list, members, sample content); Join button stays one-tap. No blind joins.
5. **Space states on cards** — Archived: collapsed section at bottom, never hidden (progress lives there). Just-created/processing: shimmer card, not absent.
6. **Scale** — sort control (last active / A–Z) past ~8 Spaces; grid collapses to list on mobile.
7. **Guided/Open badge** — now unblocked: show which situation a Space is in.
8. **Mine splits into two sections** — "Created by you" (cards show drafts pending + members active this week, not personal progress) and "Joined". Cross-Space creator dashboard = parked for Studio mode.
9. **Share a Space to a friend** — lives in Social; it's an invite in nicer wrapping. No new visibility level (three stay: Private / Invite / Public).
